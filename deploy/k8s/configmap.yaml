# 演示模式默认配置 —— 进程内存存储，无外部依赖
# 切到 real 模式时改为 APP_MODE=real 并配合 postgres 部署
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: alchemy-furnace
data:
  APP_MODE: "demo"
  DEMO_SEED: "default"
